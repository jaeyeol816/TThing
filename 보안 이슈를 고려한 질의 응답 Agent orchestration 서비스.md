보안 이슈를 고려한 질의 응답 Agent orchestration 서비스

1. Pain point
LG전자 VS사업본부의 경우, 수많은 개발자들이 하나의 프로젝트에 속해 여러 작업을 수행한다. 이와 함께 수많은 커뮤니케이션 상황이 발생하는데, 상당한 경우 책임자의 부재로 인해 딜레이가 발생한다.

이를 해결하기 위해 Meta에서 진행하는 것과 같이, 1인 1 Agent를 도입해, 질의가 필요한 경우 대상자에 직접 물어보는 대신 대상자의 Agent에 질의를 진행하는 것을 고려하고 있다.

하지만 여기에 또 하나의 문제가 발생하는데, 우리는 B2B이기 때문에, 사외 llm 서비스인 claude, gemini 등에 입력으로 포함되면 안되는 정보들이 곳곳에 존재한다. 중요한 비밀로 간주되는 협력사(oem)측의 정보를 사외 서버에 업로드 하게 되면, 수십~수백억원대 소송에 휘말리며 회사의 위신이 추락하게 된다.

여기서 충돌이 발생하는데, 대리 Agent를 구축하기 위해선 대상자의 모든 업무 내용을 Agent가 파악하고 있어야 하지만, 비밀 정보를 Agent에게 넘길 수 없다는 문제가 존재한다.

2. 해결 방안
우리 LG전자는 Exaone이라는 자체 LLM 모델을 준비하고 있으며, 현재 테스트 배포 중에 있다. Exaone은 자체 LLM 모델이기 때문에 사외 llm에 들어갈 수 없는 입력도 받을 수 있다는 이점이 있다. 하지만 성능이 claude, gemini에 비해 부족하기 때문에, 전적으로 이용하기에는 어렵다. 이를 고려하여, 사외 llm 서비스 기반인 Agent가 내부의 자료, 쿼리 등을 분석할 때, 사내 llm 서비스인 Exaone을 일종의 보안관리자로 활용해 보안성 검토, 비식별 처리 등을 진행해 사외 llm 서비스가 보안 자료의 존재와 비밀이 아닌 수준으로서의 내용을 이해할 수 있게 하거나, 접근을 차단하여 안전성을 담보하면서 동시에 정보 활용의 효용을 높이는 것을 제시한다.

3. 주요 요소
* 개인 Agent : Jira, collab, onedrive, 로컬 pc의 자료 등에 대한 정보와 대상자에 대한 정보를 md나 db의 형태로 저장/관리하는 페르소나 Agent
* GateKeeper : Exaone을 백본으로 하여, 룰베이스 알고리즘을 함께 사용해 사외 llm이 접근하려고 하는 파일/쿼리/정보가 보안 요소인지 판단하고, 마스킹을 거친 후 넘겨줄 수 있는지, 원천적으로 봉쇄해야 하는 내용인지를 함께 판단한 후 그에 따른 처리를 진행하는 Agent
* 사외 llm : 개인 Agent의 백본 llm
* ask_other_agent 도구/스킬 : 사용자는 기본적으로 본인의 Agent와 대화하면서 문제를 해결한다. 하지만 본인의 Agent가 혼자만으로 문제를 해결할 수 없는 경우, 즉 다른 Agent의 정보가 필요한 경우 다른 Agent에게 쿼리를 날려 필요한 정보를 가져오도록 한다. 또는, 사용자가 지정한 Agent에게 직접 질의를 해야 하는 경우, 사용자의 쿼리를 해당 Agent에게 넘기는 역할도 수행한다
* GUI : Tauri 기반의 웹 애플리케이션으로 일반적인 채팅 ui를 가진다. 여기에 조직도 ui를 포함해, 사용자의 Agent가 어떤 다른 Agent와 상호작용하는지도 시각화 할 수 있다.
* 보안 프로토콜 : 전사 수준의 보안 프로토콜, 팀 단위의 보안 프로토콜, 개인의 보안 프로토콜로 보안 수준의 레이어가 존재한다. Gatekeeper는 이런 보안 프로토콜을 준수해 보안 여부/비식별 처리 필요성을 판단한다. 이런 보안 프로토콜은 gui에서 수정을 할 수 있어야 한다.

4. Workflow(데모용)
1. 본 pc 안에 여러 사람의 이름으로 된 폴더가 존재한다.
    kim.chulsoo
    |------------agent
    |------------security_policy
    |------------data
    Agent 폴더에는 agent에 필요한 skill, knowledge.md 등이 존재한다.
    security_policy에는 해당 agent가 준수해야할 보안 규정이 정리되어있다. lg_policy.md, team_policy.md, personal_policy.md가 존재한다.
    data폴더에는 대상자의 업무 자료들이 위치한다. 코드, 이미지, 문서 등 다양한 파일이 존재한다.
2. 사용자는 lg전자의 어떤 직원이다. 본인의 Agent가 존재하며, 본인의 Agent와 질의응답하며 문제를 해결한다.
3. 사용지의 Agent는 스스로 해결할 수 있는 쿼리는 스스로 해결한다. 하지만 스스로 해결할 수 없는 쿼리는 다른 Agent들에 broadcasting해 도움을 요청한다.
4. 요청을 받은 Agent는 도움을 주기 위해 스스로의 data 폴더 안에 있는 자료들에 접근한다. 이때 gatekeeper가 각 security_policy를 준수해 보안 여부를 확인하고 비식별 처리를 진행한다.
5. 접근한 자료를 바탕으로 응답을 생성한 agent는 사용자의 agent에 답변을 전송한다.
6. 사용자의 agent는 답변들을 정리해 gui에 띄운다.

5. UI/UX 요구사항
1. 기본 화면은 claude, chatgpt와 같은 채팅 기반의 인터페이스이다.
2. 화면 우측에 조직도 패널이 존재해 현재 어떤 사람들의 agent에 접근 가능한지 확인할 수 있게 한다. 사용자가 누구를 클릭하여 포커싱 한 경우, 혹은 ctrl을 눌려 여러 사람을 선택한 경우 해당 사람들의 agent에 직접 쿼리를 전송한다. agent끼리 소통을 하는 경우, 해당 대상자의 요소의 테두리를 노란색으로 강조해 현재 해당 인원과 소통중임을 나타낸다.
3. 사람 이름 폴더 밑에 security_policy 폴더와 data폴더가 존재하고, build_agent 도구를 실행한 경우 해당 인원의 agent를 새로 생성하고 조직도에 추가한다.
4. 채팅 응답 말풍선 아래에 collapsable한 요소로, 질의에 대한 보안 평가, 응답 과정, 다른 agent와 소통한 경우 그 내용과 해당 agent가 응답 준비 과정에서 사용한 gatekeeper의 과정을 보기 좋게 정리하여야 한다. 이는 이 프로젝트가 가능성을 보는 PoC, MVP 프로젝트이기 때문이다.