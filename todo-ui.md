# TODO — UI / UX

채팅 인터페이스, 조직도 패널, 시각화에 관한 작업.

기획서 §5 (UI/UX 요구사항) 대응.

---

## ✅ 구현 완료

### 채팅 인터페이스 (§5.1)

- Claude/ChatGPT 스타일 채팅 레이아웃
- 사용자 말풍선 (우측 정렬, accent 배경)
- Agent 말풍선 (좌측 정렬, 테두리)
- 로딩 인디케이터 (bouncing dots)
- 자동 높이 조절 textarea (최대 120px)
- Enter 전송 / Shift+Enter 줄바꿈
- 메시지 시각 표시

### 조직도 패널 (§5.2) — 부분

- 우측 고정 패널 (300px)
- Agent 카드 — 아바타(이름 첫 글자), 이름, 전문 영역
- 활동 상태 dot — 활동중(초록) / 자리비움(노랑) / 오프라인(회색)
- 응답 후 상태 배지 — 답변 / 해당없음 / 오류
- 응답 미리보기 (2줄 clamp)
- 반응형 — 768px 이하에서 숨김

### 말풍선 상태 표시 (§5.4) — 부분

테두리 색상으로 Gatekeeper 판정 결과 표시:

| 색상 | 클래스 | 의미 |
|---|---|---|
| 초록 | `gk-pass` | 정상 통과 |
| 노랑 | `gk-masked` | SECRET — 구조 추출/가명화 적용 |
| 빨강 | `gk-blocked` | 차단 — 사내망 내부 응답 |

### Collapsible 상세 (§5.4) — 부분

- `<details>` 요소로 접이식 구현
- Summary — "Gatekeeper: 통과 / 가명화 처리됨 / 차단됨" + 등급 배지 + 검증 요약
- Body — 검증 6단계 결과 (통과/실패 아이콘 + 단계명 + 상세)
- 표현 방식 표시 (구조 추출 / 가명화 / 원문)

### 보안 프로토콜 관리 UI (§3)

- 좌우 분할 모달 (사이드바 260px + 편집 영역)
- 3계층 그룹 — 전사 / 부서·팀 / 개인
- 각 계층별 추가 버튼
- 편집 폼 — 설명, SECRET 조건(키워드/정규식/디렉토리/확장자), INTERNAL 조건, OPEN 허용 경로, EXAONE 힌트
- 섹션별 색상 구분 (빨강/노랑/초록 테두리)
- 현재 적용 규칙 미리보기
- 삭제 확인 다이얼로그

### 스타일 시스템

- CSS 변수 기반 다크 테마
- 등급별 색상 — secret(빨강) / internal(노랑) / open(초록)
- 배지는 색상 + 텍스트 라벨 병기 (색맹 대응)
- 외부 폰트·CDN 0건 (오프라인 데모 전제)
- `innerHTML` 미사용 — 전부 `createElement` + `textContent` (XSS 방지)

---

## 🔄 구현 중 / 불완전

### U1. 조직도 실시간 소통 표시

**요구사항 (§5.2)**:
> agent끼리 소통을 하는 경우, 해당 대상자의 요소의 **테두리를 노란색으로 강조**해 현재 해당 인원과 소통중임을 나타낸다

**현재**: 응답 완료 후에만 상태 표시
**목표**: broadcast 진행 중 실시간 강조

작업:
- [ ] SSE 또는 WebSocket 엔드포인트 — `GET /api/hub/events`
- [ ] 백엔드에서 이벤트 발행
  - `broadcast_start` — 대상 Agent 목록
  - `agent_querying` — 특정 Agent에 질의 시작
  - `agent_responded` — 응답 도착
  - `broadcast_end`
- [ ] 프론트에서 EventSource 구독
- [ ] `.org-card.communicating` 클래스 — 노란 테두리 + 펄스 애니메이션
- [ ] 애니메이션 — `prefers-reduced-motion` 존중

CSS 예시:
```css
.org-card.communicating {
  border-color: var(--warn);
  animation: pulse-border 1.5s ease-in-out infinite;
}
@keyframes pulse-border {
  0%, 100% { box-shadow: 0 0 0 0 rgba(255, 207, 122, 0.4); }
  50%      { box-shadow: 0 0 0 4px rgba(255, 207, 122, 0); }
}
```

### U2. 말풍선 상세 정보 확장

**요구사항 (§5.4)**:
> 채팅 응답 말풍선 아래에 collapsable한 요소로, **질의에 대한 보안 평가, 응답 과정, 다른 agent와 소통한 경우 그 내용과 해당 agent가 응답 준비 과정에서 사용한 gatekeeper의 과정**을 보기 좋게 정리

**현재**: 내 Agent의 Gatekeeper 등급/검증만
**목표**: 전체 orchestration 과정 표시

작업:
- [ ] 계층 구조로 표시
  ```
  ▶ Gatekeeper: 통과 · 사내 · 검증 6/6
    ├─ 내 Agent 처리
    │   ├─ 질문 등급: 사내
    │   ├─ 근거 문서: 3건 (기밀 1, 사내 2)
    │   ├─ 표현 변환: 가명화
    │   └─ 검증 6단계 결과
    └─ 다른 Agent 소통 (2건)
        ├─ 박선영 선임 — 답변
        │   ├─ 전달한 질문: "..."
        │   ├─ 등급 판정: 사내 (규칙 5번)
        │   ├─ 표현 변환: 가명화
        │   └─ 응답: "..."
        └─ 최민수 선임 — 해당없음
            └─ 관련성 판단: 전문 영역 불일치
  ```
- [ ] `HubAskResponse`에 상세 정보 추가 필요 (백엔드 협업)
- [ ] 중첩 `<details>` 또는 트리 UI
- [ ] 페이로드 전문 보기 (생략 없이 — BR-U-01)

---

## ❌ 구현 필요

### U3. Agent 직접 선택 질의

**요구사항 (§5.2)**:
> 사용자가 누구를 클릭하여 포커싱 한 경우, 혹은 **ctrl을 눌러 여러 사람을 선택**한 경우 해당 사람들의 agent에 직접 쿼리를 전송

작업:
- [ ] 조직도 카드 클릭 이벤트 — 선택 토글
- [ ] Ctrl(Cmd)+클릭 — 다중 선택
- [ ] 선택 상태 시각화 — 테두리 accent + 배경 강조
- [ ] 선택 해제 — 빈 영역 클릭 또는 ESC
- [ ] 선택된 대상 수 표시 — 입력창 위 또는 조직도 헤더
- [ ] 전송 시 `targets` 배열 포함
- [ ] 선택 없으면 broadcast (기존 동작)
- [ ] 전송 후 선택 초기화 여부 결정

### U4. build_agent UI

**요구사항 (§5.3)**:
> 사람 이름 폴더 밑에 security_policy 폴더와 data폴더가 존재하고, **build_agent 도구를 실행한 경우 해당 인원의 agent를 새로 생성**하고 조직도에 추가

작업:
- [ ] 조직도 하단 "Agent 추가" 버튼
- [ ] 모달 — 폴더 스캔 결과 표시
  - 발견된 폴더 목록
  - 각 폴더의 `data/`, `security_protocol/` 존재 여부
  - Agent로 등록 가능한 것만 선택 가능
- [ ] 이름 / 전문 영역 입력 (또는 자동 추론 결과 확인)
- [ ] 생성 진행 표시
- [ ] 성공 시 조직도 자동 갱신

### U5. 사용자 전환 UI 복원

**현재**: 제거됨 (요청에 따라)
**필요성**: 데모에서 여러 사람 관점을 보여줘야 함

작업:
- [ ] 조직도 헤더 또는 별도 위치에 현재 사용자 표시
- [ ] 전환 방법 — 드롭다운 또는 조직도에서 "나로 전환"
- [ ] 전환 시 조직도 갱신 (자기 자신 제외)
- [ ] 채팅 히스토리 유지 여부 결정

### U6. 채팅 히스토리 / 세션 관리

**현재**: 새로고침 시 대화 사라짐

작업:
- [ ] localStorage에 대화 저장
- [ ] 세션 목록 (좌측 패널 또는 드롭다운)
- [ ] 새 대화 시작 버튼
- [ ] 대화 삭제

**우선순위 낮음** — 데모에서는 단일 세션으로 충분

### U7. 접근성 개선

- [ ] 조직도 카드 키보드 네비게이션 (Tab, 화살표)
- [ ] 선택 상태 `aria-selected`
- [ ] 실시간 상태 변경 `aria-live` 영역
- [ ] 모달 포커스 트랩
- [ ] 색상 대비 재검증 (새로 추가한 색상)

### U8. Tauri 데스크톱 셸

- [ ] `app/` 디렉터리 Tauri 설정 검증
- [ ] 백엔드 자동 기동 확인
- [ ] 창 크기 / 타이틀 설정
- [ ] 빌드 테스트 (`make app`)

**우선순위 낮음** — 브라우저로 데모 가능

---

## 개선 아이디어 (요구사항 외)

- 조직도에 소통 경로 시각화 — 내 Agent → 다른 Agent 연결선
- 답변 생성 중 단계별 진행 표시 (등급 판정 → 변환 → 검증 → 전송 → 재수화)
- 감사 로그 탭 부활 — "이 문구는 한 번도 경계를 넘지 않았습니다" 검색
- 문서 업로드 UI 부활 — 드래그 앤 드롭 + 즉시 등급 판정 결과 표시
- 다크/라이트 테마 토글

---

## 알려진 이슈

| 이슈 | 영향 | 대응 |
|---|---|---|
| `test_lint_web` 실패 — `role="tablist"` 없음 | 테스트 | UI 구조 변경에 맞춰 lint 규칙 수정 필요 |
| 답변에 `[사내 · 사내망 밖으로 나간 것 없음]` 접두사 노출 | UX | 프론트에서 파싱해 배지로 분리 표시 |
| 조직도에서 자기 자신(currentUser) 제외 로직이 currentUser 없으면 전체 표시 | 경미 | 초기 로드 순서 확인 |
| 모바일에서 조직도 접근 불가 (숨김) | 반응형 | 하단 시트 또는 탭으로 전환 |
